TUNE_FLAGS="-march=native -mcpu=native"

CFLAGS="${TUNE_FLAGS} ${CFLAGS}"
CXXFLAGS="${TUNE_FLAGS} ${CPPFLAGS} ${CXXFLAGS}"
CPPFLAGS="${CXXFLAGS} ${CPPFLAGS}"
FFLAGS="${TUNE_FLAGS} ${FFLAGS}"
FCFLAGS="${TUNE_FLAGS} ${FCFLAGS}"
