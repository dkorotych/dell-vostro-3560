CFLAGS="-march=ivybridge -O2 -pipe"
CXXFLAGS="${CFLAGS}"
CPPFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"
FCFLAGS="${FFLAGS}"

CPU_FLAGS_X86="aes avx mmx mmxext popcnt sse sse2 sse3 sse4_1 sse4_2 ssse3"
#MAKEOPTS="${MAKEOPTS} -j5 -l4"
#EMERGE_DEFAULT_OPTS="${EMERGE_DEFAULT_OPTS} --jobs=5 --load-average=4"

VIDEO_SUPPORT="vaapi vulkan"
VIDEO_CARDS="intel i965"
ALSA_CARDS="hda-intel usb-audio"

USE="${CPU_FLAGS_X86} ${VIDEO_SUPPORT}"
